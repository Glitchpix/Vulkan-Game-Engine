enable_testing()
add_executable(Tests src/doctest_main.cpp)
target_include_directories(Tests PRIVATE ${CMAKE_SOURCE_DIR}/tests/src)
list(APPEND CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/cmake")
include(CTest)
include(doctest)
doctest_discover_tests(Tests)